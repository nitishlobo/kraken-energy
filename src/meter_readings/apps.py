"""Meter readings app."""

from django.apps import AppConfig


class MeterReadingsConfig(AppConfig):
    """Meter readings config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "meter_readings"
